version: "3"
services:
  backend:
    build: .
    ports:
      - "9001:9001"
  mysql:
    platform: linux/x86_64
    image: mysql:5.7.20
    environment:
      - MYSQL_ROOT_PASSWORD=admin
    volumes:
      - ../database-model:/docker-entrypoint-initdb.d